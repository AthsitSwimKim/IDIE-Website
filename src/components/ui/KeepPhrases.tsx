import { Fragment } from 'react'

/**
 * ศัพท์ไทยที่ห้ามถูกตัดขึ้นบรรทัดใหม่กลางวลี
 *
 * ทุกคำในรายการนี้เป็น**ศัพท์เฉพาะด้านความปลอดภัยหรือชื่อระบบ** ที่ถ้าครึ่งหนึ่ง
 * ไปอยู่คนละบรรทัด ผู้อ่านต้องปะติดปะต่อความหมายเอง เช่น "พื้นที่ | อันตราย"
 * ที่บรรทัดแรกจบด้วย "พื้นที่" เฉย ๆ ทำให้ตาอ่านผ่านไปโดยยังไม่รู้ว่าเป็นพื้นที่แบบใด
 *
 * **เกณฑ์การเพิ่มคำ** — ต้องเป็นวลีที่แยกแล้วเสียความหมาย ไม่ใช่แค่คำยาว
 * และต้องสั้นพอที่จะไม่กว้างเกินกล่องที่แคบที่สุดที่มันไปปรากฏ (นับที่จอ 320px)
 * เพราะ `whitespace-nowrap` ที่ยาวเกินกล่องจะ**ล้นออกนอกกรอบ**แทนที่จะตัด
 *
 * เรียงจากยาวไปสั้น เพื่อให้วลีที่ยาวกว่าถูกจับคู่ก่อนเสมอ —
 * "ชนิดกันระเบิด" ต้องชนะ "กันระเบิด" ไม่งั้นจะเหลือ "ชนิด" ค้างท้ายบรรทัดเหมือนเดิม
 */
const KEEP_TOGETHER = [
  /*
    วลีท้ายประโยคนำของหมวด Engineering Highlights — ยาวกว่าตัวอื่นในรายการมาก
    แต่ยังผ่านเกณฑ์ 320px (วัดแล้วกว้าง 245px ในกล่อง 280px) ที่ต้องคุมทั้งก้อนเพราะ
    เจ้าของระบบต้องการให้ทั้งวลีลงไปอยู่บรรทัดที่สอง ไม่ใช่ตัดกลางคำว่า "ส่งมอบ"
  */
  'จนถึงการติดตั้งและดูแลหลังส่งมอบ',
  /*
    วลีปิดประโยคนำหน้าร่วมงานกับเรา — ตัดตามพจนานุกรมแล้วได้ "…ชั้นนำระดับ / ประเทศ"
    บรรทัดแรกจบด้วยคำว่า "ระดับ" ที่ยังไม่บอกว่าระดับอะไร แล้วเหลือ "ประเทศ" ยืนอยู่
    บรรทัดสุดท้ายคำเดียว กว้าง 207px ที่ตัวอักษร 19px ยังผ่านเกณฑ์กล่องแคบสุด 272px
  */
  'โครงการชั้นนำระดับประเทศ',
  'ระบบควบคุมการเข้าออก',
  /*
    ไม่ใส่แล้วเบราว์เซอร์ตัดเป็น "…และระบบดับ / เพลิง" ซึ่งบรรทัดแรกจบด้วยคำว่า
    "ระบบดับ" ที่อ่านแล้วเข้าใจไปคนละเรื่อง จุดตัดนี้ถูกต้องตามพจนานุกรม
    (ดับ + เพลิง เป็นคนละคำ) แต่ผิดความหมายที่ต้องการสื่อ
  */
  'ระบบดับเพลิง',
  /* ชื่อประเภทโรงงาน — ตัดแล้วเหลือ "โรงงาน" ลอยท้ายบรรทัดโดยยังไม่บอกว่าโรงงานอะไร */
  'โรงงานปิโตรเคมี',
  'โรงงานเหล็ก',
  'อุปกรณ์ส่งสัญญาณ',
  /* คู่กับ "เสียง" เสมอในเว็บนี้ — ตัดแล้วเหลือ "แสง" ลอยท้ายบรรทัด */
  'แสงสัญญาณ',
  /* คำกริยาคำเดียว เบราว์เซอร์ตัดกลางเป็น "ตรวจ / สอบ" ได้ตามพจนานุกรม */
  'ตรวจสอบ',
  'อะไหล่',
  'ระบบประกาศ',
  /*
    ประโยคนำหน้าบริการขึ้นประโยคที่สองด้วยคำนี้ — ปล่อยไว้เบราว์เซอร์ตัดเป็น
    "…บริการหลังการขาย ทุก / กลุ่มงาน" บรรทัดแรกจึงจบด้วยคำว่า "ทุก" ที่ยังไม่บอกว่า
    ทุกอะไร และการคุมก้อนนี้ยังตรึงจุดตัดของบรรทัดแรกไว้ที่ท้ายคำว่า "ขาย" พอดี
    ทำให้ "ขาย" ไม่หลุดไปอยู่ต้นบรรทัดที่สองตามลำพัง
  */
  'ทุกกลุ่มงาน',
  'สัญญาณเตือนภัย',
  /*
    "พื้นที่" ที่ค้างท้ายบรรทัดไม่บอกอะไรเลยว่าเป็นพื้นที่แบบไหน — รวมวลีที่ใช้จริง
    ในเว็บไว้ที่นี่ ไม่ได้จับ "พื้นที่" + คำถัดไปแบบอัตโนมัติ เพราะภาษาไทยไม่มีช่องว่าง
    ระหว่างคำ การจับแบบนั้นจะลากยาวจนก้อนกว้างเกินกล่องแล้วล้นออกนอกกรอบ
  */
  /* ตัดหลัง "การ" แล้วบรรทัดจบด้วยคำนำหน้าที่ไม่มีความหมายในตัวเอง */
  'การจำแนกพื้นที่จริง',
  'พื้นที่จำแนกอันตราย',
  'พื้นที่อุตสาหกรรม',
  'พื้นที่อันตราย',
  'พื้นที่โรงงาน',
  'พื้นที่ใช้งาน',
  'พื้นที่ผลิต',
  'พื้นที่จริง',
  'ชนิดกันระเบิด',
  'กล้องวงจรปิด',
  'ระบบเครือข่าย',
  'ระบบโทรศัพท์',
].sort((a, b) => b.length - a.length)

/**
 * คำเชื่อมที่ต้องติดไปกับวลีข้างหลังเสมอ
 *
 * ภาษาไทยไม่มีช่องว่างระหว่างคำ เบราว์เซอร์จึงตัดหลัง "และ" ได้ตามพจนานุกรม
 * ผลคือบรรทัดจบด้วยคำว่า "และ" โดด ๆ แล้วค่อยขึ้นของที่มันเชื่อมในบรรทัดถัดไป
 * ซึ่งอ่านสะดุดทุกครั้ง — พบบนพาดหัวย่อยหน้าแรก "…กล้องวงจรปิด และ / ระบบควบคุมการเข้าออก"
 *
 * "พร้อม" อยู่ในกลุ่มนี้ด้วยเพราะทำหน้าที่เดียวกัน — "…ดูแลหลังส่งมอบ พร้อม / อะไหล่"
 * ทำให้บรรทัดจบด้วยคำที่ยังค้างอยู่ว่าพร้อมอะไร
 *
 * ผูกไว้กับวลีใน KEEP_TOGETHER เท่านั้น ไม่ได้ห้ามตัดหลังคำเชื่อมทั้งเว็บ —
 * การห้ามทั้งหมดจะทำให้ก้อนข้อความยาวเกินกล่องแคบ ๆ แล้วล้นออกนอกกรอบแทน
 */
const LEADING_CONJUNCTIONS = ['และ', 'หรือ', 'พร้อม']

const PATTERN = new RegExp(
  `((?:${LEADING_CONJUNCTIONS.join('|')})?(?:${KEEP_TOGETHER.join('|')}))`,
  'g',
)

export interface KeepPhrasesProps {
  /** ข้อความล้วน — ห้ามส่ง element เข้ามาเพราะต้องค้นหาวลีในสตริงจริง */
  children: string
}

/**
 * กันไม่ให้วลีศัพท์เฉพาะภาษาไทยถูกตัดคนละบรรทัด
 *
 * ต่างจาก `KeepWords` ที่กันการตัด**กลางคำเดียว** (ชื่อเฉพาะที่พจนานุกรมไม่รู้จัก
 * แล้วเบราว์เซอร์เดาตัดผิด เช่น "อินดัสเตรี | ยล") ตัวนี้กันการตัด**ระหว่างคำที่
 * ต้องอ่านด้วยกัน** ซึ่งเป็นจุดตัดที่ถูกต้องตามพจนานุกรมทุกประการ แต่อ่านแล้วสะดุด
 *
 * **ทำไมต้องเป็น span ไม่ใช่ CSS** — ทดสอบบนหน้าจริงแล้วทั้ง `word-break: keep-all`,
 * `word-break: break-word`, `line-break: strict` และค่า `normal` ให้ผลการตัดบรรทัด
 * **เหมือนกันทุกตัวอักษร** เบราว์เซอร์ตัดบรรทัดไทยด้วยพจนานุกรมในตัวซึ่ง CSS
 * ไม่มีคุณสมบัติไหนสั่งได้ ต้องบอกเป็นราย "ก้อน" ว่าห้ามตัดข้างในเท่านั้น
 *
 * ใช้กับข้อความที่เป็นสตริงเดียวเท่านั้น ไม่รองรับ element ซ้อน —
 * ถ้าวันหนึ่งต้องใส่ตัวหนาหรือลิงก์ในประโยคเดียวกัน ต้องแยกเป็นหลายชิ้นแล้วครอบทีละชิ้น
 */
export function KeepPhrases({ children }: KeepPhrasesProps) {
  const parts = children.split(PATTERN)

  return (
    <>
      {parts.map((part, index) =>
        // ชิ้นที่ index เป็นเลขคี่คือกลุ่มที่ถูกจับด้วยวงเล็บใน regex = วลีที่ต้องคุม
        index % 2 === 1 ? (
          <span key={`${index}-${part}`} className="whitespace-nowrap">
            {part}
          </span>
        ) : (
          <Fragment key={`${index}-${part}`}>{part}</Fragment>
        ),
      )}
    </>
  )
}
